package team;

import java.util.ArrayList;

public interface DataOP {
	abstract float Average(ArrayList<?> list);
	abstract float Max(ArrayList<?> list);
	abstract float Min(ArrayList<?> list);
}
